import { Theme, ThemeMode } from "@crayonai/react-ui/ThemeProvider";

export const theme = __LIGHT_THEME_CONFIG__ as Theme;
export const darkTheme = __DARK_THEME_CONFIG__ as Theme;
export const themeMode = __THEME_MODE__ as ThemeMode;